# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/anzo_insurance_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root123
  redis:
    host: localhost
    port: 6379
    password: 
  mail:
    host: localhost
    port: 1025
  jackson:
    serialization:
      indent-output: true
      
app:
  file:
    storage:
      type: local  # 开发环境使用本地存储
      local:
        base-path: ./uploads
        base-url: http://localhost:8080/api/files

# 开发环境日志级别
logging:
  level:
    com.anzo.insurance: DEBUG
    org.springframework.security: DEBUG
    org.springframework.transaction: DEBUG