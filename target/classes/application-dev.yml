# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://193.168.3.183:3306/anzo_insurance_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
  redis:
    host: 192.168.3.183
    port: 6379
    password: 
  # Redis 本地开发模板，按需取消注释
  # redis:
  #   host: 127.0.0.1
  #   port: 6379
  #   password:
  #   database: 0
  #   timeout: 3000ms
  # elasticsearch:
  #   uris: http://localhost:9200
  #   username:
  #   password:
  # Elasticsearch 本地开发模板，按需取消注释
  # elasticsearch:
  #   uris: http://127.0.0.1:9200
  #   username: elastic
  #   password: your-es-password
  #   connection-timeout: 3s
  #   socket-timeout: 10s
  mail:
    host: localhost
    port: 1025
  jackson:
    serialization:
      indent-output: true
      
app:
  file:
    storage:
      type: local  # 开发环境使用本地存储
      local:
        base-path: ./uploads
        base-url: http://localhost:8080/api/files

# 开发环境日志级别
logging:
  level:
    com.anzo.insurance: DEBUG
    org.springframework.security: DEBUG
    org.springframework.transaction: DEBUG
